import app from '../../dist/server/app'

describe('app.js', () => {
	it('should listen on process.env.PORT', () => {
		
	})

	describe('/data/batch', () => {
		it('should accept GET requests', () => {
			
		})

		it('should respond with a JSON object', () => {

		})

		it('should not take longer than 10s', () => {
			
		})
	})
})