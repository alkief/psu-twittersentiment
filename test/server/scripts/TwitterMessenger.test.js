import TwitterMessenger from '../../../dist/server/scripts/TwitterMessenger'

describe('TwitterMessenger.js', () => {
	describe('constructor', () => {
		it('should instantiate a Twitter client member variable', () => {

		})
	})

	describe('searchSet', () => {
		it('should accept an array of JSON objects representing search parameters', () => {

		})

		it('should return an array of JSON objects representing tweets', () => {
			
		})

		it('should execute in under 3s', () => {

		})
	})

	describe('search', () => {
		it('should accept a single JSON object as an argument', () => {

		})

		it('should return an array of JSON objects representing tweets', () => {
			
		})

		it('should execute in under 3s', () => {
			
		})
	})
})